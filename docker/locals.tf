locals {
  # Host-specific configurations have been moved to hosts.tf
  # Shared configurations are in their respective files:
  # - env.tf: Environment variable templates
  # - config_traefik.tf: Traefik configuration
  # - config_tsdproxy.tf: TSDProxy configuration (if it exists)
}